---
import { Icon } from "astro-icon";

const { url, text, class: className = "" } = Astro.props;
---

<p>
  <a href={url} class:list={["flex", "underline", "mb-4", className]}
    ><Icon name="carbon:arrow-left" class="w-[20px] mr-2" />{text}</a
  >
</p>
